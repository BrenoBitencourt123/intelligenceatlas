Add `text-right` to the className in the `formatInline` function's `[cite]` regex replacement in `src/components/atlas/MarkdownText.tsx` (around line 113).

Single-line change: `block mt-1` becomes `block text-right mt-1`.